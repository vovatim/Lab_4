# Здесь необходимо реализовать
# контекстный менеджер json_from_file,
# который принимает аргумент path - относительный путь до файла на диске
# Пример использования
# with json_from_file('data_light.json') as data:
#   print(data[0].get('job-name'))
